services:
  - type: web
    name: miobrafacil-api
    env: docker
    runtime: php
    rootDir: backend
    buildCommand: echo "No build needed"
    startCommand: php -S 0.0.0.0:$PORT -t .
    envVars:
      - key: PHP_VERSION
        value: "8.2"
